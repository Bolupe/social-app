import { CreateForm } from "./create-form";
// Create form as a reuseable component that takes all input as a single post
// and transforms that post to an individual post to be viewed on the home page


export const CreatePost = () => {
  return (
    // Create form in th 
    <div>
      <CreateForm />
    </div>
  );
};
